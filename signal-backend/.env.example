ANTHROPIC_API_KEY=sk-ant-...
ELEVENLABS_API_KEY=sk_...
CLAUDE_MODEL=claude-opus-4-8
# eleven_v3 renders inline audio tags ([laughs], [sighs]) as real delivery.
# Rollback: set eleven_multilingual_v2 — the pipeline strips tags automatically.
ELEVENLABS_MODEL=eleven_v3
STORAGE_PATH=./data
# Optional: robust article extraction for JS-rendered/paywalled pages (firecrawl.dev)
FIRECRAWL_API_KEY=fc-...
# Optional: require this token on all API/media requests (recommended when hosted)
SIGNAL_API_TOKEN=
